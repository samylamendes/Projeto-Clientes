package logica;
import java.util.UUID;

public interface OrgCadastro<TIPO> {
	void salvar(TIPO cliente);
	TIPO buscar(UUID codigo);
	void deletar(UUID codigo);
	void atualizar(TIPO objetoAtualizar);
}
